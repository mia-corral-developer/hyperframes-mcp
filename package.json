{
  "name": "hyperframes-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted MCP server that wraps the HyperFrames CLI (render, snapshot, lint, compositions, init) for agents.",
  "bin": { "hyperframes-mcp": "src/index.js" },
  "scripts": {
    "start": "node src/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.23.8"
  }
}